# Optional GPU backend (NVIDIA, CUDA 12.x+ driver; Blackwell/RTX 50-series OK).
# The [ctk] extra bundles the CUDA headers/nvrtc so no system CUDA install is
# needed. The solver runs identical code on this backend via array_module=cupy.
cupy-cuda12x[ctk]>=14.0
# 3D vortex visualisation (render_3d.py): marching-cubes isosurfaces.
scikit-image>=0.22
# Real-time interactive 3D viewer (live_viewer_3d.py): VTK-based rendering.
pyvista>=0.45
